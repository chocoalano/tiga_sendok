import 'package:flutter/material.dart';

const padding = 20.0;
const base_Url = 'https://test.goldenmom.id/api';

// colors config
const primary = Color.fromARGB(255, 0, 0, 0);
const secondary = Color.fromARGB(255, 49, 49, 49);
const danger = Color.fromARGB(255, 255, 0, 0);
const success = Color.fromARGB(255, 0, 190, 51);
